__default__:
    partition: norm
    time: 24:00
    jobname: "Snake.{rule}.{wildcards}"
    extra: "--gres=lscratch:100"
    output: "./logs/pre_process/{rule}.{wildcards}.stdout"
    error: "./logs/pre_process/{rule}.{wildcards}.stderr"
